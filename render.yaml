# render.yaml (for Render.com deployment)
services:
  - type: web
    name: barnacle-films
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: CREW_PASSWORD
        value: STUDIO!@#
      - key: DATABASE_URL
        fromDatabase:
          name: barnacle-films-db
          property: connectionString

databases:
  - name: barnacle-films-db
    databaseName: barnacle_films
    user: barnacle_user

